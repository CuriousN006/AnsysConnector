# Workflow plans

This directory now holds the low-level, action-by-action plan format used for advanced orchestration and debugging.

If you want the official Fluent v1 surface, start with the workflow specs under `examples\workflows\fluent\`.
Use `run-plan` here only when you need manual session control, named session handles, or custom low-level action sequencing.
