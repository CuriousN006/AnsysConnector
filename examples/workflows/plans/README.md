# Workflow plans

This directory is reserved for higher-level reusable workflow templates that orchestrate multiple product adapters. Product-scoped examples currently live under `examples/products/`.
