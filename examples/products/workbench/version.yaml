name: workbench-version-check

sessions:
  workbench:
    adapter: workbench
    profile: safe
    workspace: runs/workbench-version-check
    options:
      show_gui: false

steps:
  - session: workbench
    action: version
