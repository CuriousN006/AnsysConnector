name: workbench-version-check

adapters:
  workbench:
    profile: safe
    workspace: runs/workbench-version-check
    options:
      show_gui: false

steps:
  - adapter: workbench
    action: version
